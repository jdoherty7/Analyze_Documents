try:
	from setuptools import setup
except ImportError:
	from distutils.core import setup

config = {
	'description': 'a basic directory for testing',
    'author': 'John Doherty',
    'url': 'URL',
    #'download_url': 'Where to download it.',
    'version': '0.1',
    'install_requires': ['nose'],
    #folder named NAME in the basic folder
	'packages': ['NAME'],
    'name': 'basic'

}

setup(**config)